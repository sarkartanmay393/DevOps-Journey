terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~>4.0"
    }
  }
}

provider "aws" {
  region = "us-east-1"
}

# S3 Bucket with encryption.
resource "aws_s3_bucket" "rbs_s3" {
  bucket = "rbs-bucket-sarkartanmay393"
  lifecycle {
    prevent_destroy = true
  }
  versioning {
    enabled = true
  }
  server_side_encryption_configuration {
    rule {
      apply_server_side_encryption_by_default {
        sse_algorithm = "AES256"
      }
    }
  }
}

# Locking with DynamoDB
resource "aws_dynamodb_table" "lock_state" {
  name         = "lock-state-sarkartanmay393"
  billing_mode = "PAY_PER_REQUEST"
  hash_key     = "LockID"
  attribute {
    name = "LockID"
    type = "S"
  }
}


